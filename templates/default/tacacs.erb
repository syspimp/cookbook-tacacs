# Generated by Chef.
# Local modifications will be overwritten.

# This is the configuration file for /etc/init.d/tacacs+
# You can overwrite default arguments passed to the daemon here.
# See man(8) tac_plus

DAEMON_OPTS="-C /etc/tacacs+/tac_plus.conf -B <%= node['network']['sw_mgmt_ipv4'] %>"
LOGDIR=<%= node['tacacs']['log_dir'] %>
PIDFILE=/var/run/tac_plus.pid.<%= node['network']['sw_mgmt_ipv4'] %> # necessary due to '-B' above.
